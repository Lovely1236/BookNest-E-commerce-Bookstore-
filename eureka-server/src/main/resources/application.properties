# Eureka Server Port
server.port=8761

# App name
spring.application.name=eureka-server

# IMPORTANT (server should NOT register itself)
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# Optional (clean URL)
eureka.instance.hostname=localhost